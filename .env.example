# -----------------------------------------------------------------------------
# REQUIRED CONFIGURATION (all these variables must be set for the agent to work)
# -----------------------------------------------------------------------------

# Your OpenAI API key - Required to connect to OpenAI's API services
# Get it from: https://platform.openai.com/api-keys
OPENAI_API_KEY=""

# -----------------------------------------------------------------------------
# DIRECTORY CONFIGURATION (define which directories the agent can access)
# -----------------------------------------------------------------------------

# Uncomment and set paths to directories you want the agent to have access to
# The agent will ONLY be able to access these directories (plus the current working dir)
# MERLIN_DIR_HOME=/path/to/home
# MERLIN_DIR_WORK=/path/to/project
# MERLIN_DIR_CODE=/path/to/code
# You can add more custom directories with the MERLIN_DIR_ prefix

# -----------------------------------------------------------------------------
# ADDITIONAL CONFIGURATION (optional settings with defaults)
# -----------------------------------------------------------------------------

# The OpenAI model to use - Different models have different capabilities and costs
# Options: gpt-4o, gpt-4, gpt-3.5-turbo, etc.
MODEL_NAME=""

# The initial instructions that define how the AI agent behaves
# This sets the personality and capabilities of the agent
SYSTEM_PROMPT=""

# Minimum similarity score (0-100) for fuzzy file search matches
# Higher values require more exact matches (recommended: 50-80)
FUZZY_SEARCH_THRESHOLD=60

# Default number of results to return from file searches
# How many matches to show when searching for files
DEFAULT_SEARCH_RESULTS=3